<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "TAVORE",
  "short_name": "TAVORE",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "tavore_logo_02.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#000000",
  "background_color": "#05627B",
  "display": "fullscreen",
  "orientation": "portrait"
}
