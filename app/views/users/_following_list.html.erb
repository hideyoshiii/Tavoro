<% if params[:q].blank? %>
	<% if @users.present? %>
	  	<% @users.each do |user| %>
	  		<% unless user == current_user %>
		  		<a href="" data-toggle="modal" data-target="#myModal_profile_<%= user.id %>">
		          <div class="box_tests05">
		            <div class="pro21">
			            <%= image_tag user.image(:medium), :class =>'img_user09' %>
		            </div>
		            <div class="pro22">  
		              	<span class="font_white_max">
		                	<%= user.name %>
		              	</span>
		            </div>
		            <div class="pro23" style="text-align:right;padding-right:10px;">
		            	<div class="font_gray_max">
			              <i class="fas fa-chevron-right"></i>
			            </div>
		            </div>
		          </div>
		        </a>      
	        <% end %>
	  	<% end %>
	<% else %>
	 	<div class="font_gray" style="margin-top:30vh;text-align:center;">
			<%= image_tag "test28.png", :style =>'width:20px;height:20px;' %> フォローしているユーザーがいません
		</div>
	<% end %>
<% else %>
	<% if @items.present? %>
	  	<% @items.each do |user| %>
	  		<% unless user == current_user %>
		  		<a href="" data-toggle="modal" data-target="#myModal_profile_<%= user.id %>">
		          <div class="box_tests05">
		            <div class="pro21">
			            <%= image_tag user.image(:medium), :class =>'img_user09' %>
		            </div>
		            <div class="pro22">  
		              	<span class="font_white_max">
		                	<%= user.name %>
		              	</span>
		            </div>
		            <div class="pro23" style="text-align:right;padding-right:10px;">
		            	<div class="font_gray_max">
			              <i class="fas fa-chevron-right"></i>
			            </div>
		            </div>
		          </div>
		        </a>	        
	        <% end %>
	  	<% end %>
	<% else %>
		<div class="font_gray" style="margin-top:30vh;text-align:center;">
			<%= image_tag "test28.png", :style =>'width:20px;height:20px;' %> 結果がありません
		</div>
	<% end %>
<% end %>